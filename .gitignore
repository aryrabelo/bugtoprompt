node_modules
dist
.bugtoprompt/

# Local agent / harness state — never publish
.claude/
.harness-mem/
docs/superpowers/
extension/*.zip

# Worker/conductor scratch — lives in a worktree's .context/, never the repo
.context/
RUN-LOG.md
handoff.md
DONE
